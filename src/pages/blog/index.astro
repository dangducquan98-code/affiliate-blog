---
export const prerender = false;

import BaseLayout from '../../layouts/BaseLayout.astro';
import PostCard from '../../components/PostCard.astro';
import { listPublishedPosts } from '../../lib/posts';

const { posts, configured, error } = await listPublishedPosts();
---

<BaseLayout
  title="Blog"
  description="Bài viết làm TikTok kiếm tiền affiliate + review gear quay: hook, kịch bản, lộ trình 7 ngày, setup mic/đèn — Quân Kiu kể thật."
  canonicalPath="/blog"
>
  <section class="container">
    <h1>Blog</h1>
    <p style="color: var(--ink-muted); max-width: 36rem;">
      Bài để bạn làm được việc: viết hook, dựng kịch bản, bắt đầu affiliate, chọn mic/đèn khi cần — không catalogue bán hàng.
    </p>
    {
      error ? (
        <p class="fallback-notice" style="margin-top: 1.5rem;">{error}</p>
      ) : posts.length === 0 ? (
        <p class="fallback-notice" style="margin-top: 1.5rem;">
          {configured
            ? 'Chưa có bài published. Vào /admin để tạo bài hoặc publish.'
            : 'Supabase chưa cấu hình — danh sách bài sẽ xuất hiện sau khi điền env và import dữ liệu.'}
        </p>
      ) : (
        <ul class="post-list" style="margin-top: 1.5rem;">
          {posts.map((post) => (
            <PostCard
              href={`/blog/${post.slug}`}
              title={post.title}
              description={post.description}
              date={new Date(post.created_at)}
              category={post.category}
              coverImage={post.cover_image}
            />
          ))}
        </ul>
      )
    }
  </section>
</BaseLayout>
