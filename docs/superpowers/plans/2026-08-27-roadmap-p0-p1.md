# Roadmap P0 + early P1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship approved roadmap items P0-2/3/4/5 and P1-6/7/10/11 (skip item 1 + 12) on `feature/blog-upgrade`.

**Architecture:** Click events via Supabase service-role insert from `/go` (fire-and-forget); public pages get short CDN cache; content upserts via scripts; admin publish gate checks catalog `affiliate_url`.

**Tech Stack:** Astro 7 + TypeScript + Supabase + Vercel

**Spec:** User brief 2026-08-27 + `docs/blog-roadmap.md` (items 2–7, 10–11)

## Global Constraints

- Branch: `feature/blog-upgrade` (commits `7438654`, `7dc2ba9` present)
- Migration SQL written only — Hermes runs later
- No push; English commits (2–3); no `.env.local` / `AGENTS.md` / item 1 / item 12
- Voice: `docs/voice-profile-quankiu.md` + `docs/content-quality-notes.md`

---

### Task 1: P0-3 Click tracking `/go`

**Files:**
- Create: `supabase/migrations/20260827_click_events.sql`
- Create: `src/lib/click-events.ts`
- Modify: `src/pages/go/[slug].ts`
- Modify: `src/pages/admin/index.astro`

- [ ] Migration: `click_events` table + index `(slug, created_at desc)`; RLS enabled, **no anon policies** (service role only)
- [ ] `logGoClick({ slug, referrer, utm_* })` fire-and-forget insert via service client
- [ ] `/go` reads query + Referer header, logs then redirects
- [ ] Admin: stat card “Click /go 7 ngày” + top 10 table + total

### Task 2: P0-4 `/links` + UTM docs + P0-5 homepage + P0-2 deals

**Files:**
- Create: `src/pages/links.astro`, `docs/utm-convention.md`
- Modify: `src/pages/index.astro`, `src/data/deals.yaml`

- [ ] `/links` mobile-first ≤5 posts (hook / kịch bản / hậu trường / 7 ngày / 20 món)
- [ ] UTM convention doc
- [ ] Homepage hero + meta → TikTok affiliate + gear; CTA → `/blog` or `/links`; deals secondary
- [ ] Replace charger/hub placeholder deals with mic/đèn/tripod gear

### Task 3: P1-6 RelatedPosts + internal links + P1-7 pillar

**Files:**
- Create: `src/components/RelatedPosts.astro`
- Create: `scripts/content/lam-tiktok-affiliate-tu-0.md`
- Create: `scripts/upsert-pillar-and-internal-links.ts` (or split)
- Modify: `src/pages/blog/[slug].astro`, `src/lib/posts.ts`, `package.json`

- [ ] `listRelatedPublishedPosts(slug, category, limit)`
- [ ] RelatedPosts block 2–3 at end of post
- [ ] Script: inject ≥2 natural internal links per existing post + upsert pillar `lam-tiktok-affiliate-tu-0`

### Task 4: P1-11 publish gate + P1-10 cache

**Files:**
- Create: `src/lib/publish-guard.ts` (+ test)
- Modify: `src/pages/api/admin/posts.ts`, `src/pages/api/admin/posts/[id].ts`
- Modify: admin products UI copy
- Create: `src/middleware.ts` (or per-page headers)

- [ ] On `published=true`, resolve attached product slugs; if any missing `affiliate_url` → 400
- [ ] Toast already shows API error (verify)
- [ ] Stronger “chưa dán link” on products list/form
- [ ] Public HTML Cache-Control 60–300s + SWR; `/go` `/admin` `/api` no-store

### Task 5: Verify + commit

- [ ] `npm run build` PASS
- [ ] Smoke `/`, `/links`, `/go/<slug>`, RelatedPosts, publish guard
- [ ] 2–3 English commits, no push
